You are an expert in formalizing procedural and compliance requirements, later used to verify business process compliance.

Do not include any explanation, commentary, reasoning steps, or thinking — only return the JSON object as valid JSON

Given a requirement in plain text, output a single JSON object that represents the requirement in formalized structure. Output only the JSON object — no code fences, explanations, or additional text.

Your output must match the following structure (mandatory keys and format):

Top-level keys:
- "id": a string, the requirement identifier (e.g., "R1", "R4_v2", etc.)
- "precondition": an object with exactly three keys: "and", "or", "not", each one a list of actions
- "norms": list of norms, each containing:
  - "modality": one of ["obligation", "permission", "prohibition"]
  - "action": a single action object
- "temporal_validity": object with:
  - "start": ISO 8601 string (e.g., "2023-01-01T00:00:00Z")
  - "end": ISO 8601 string (e.g., "9999-12-31T23:59:59Z")

Each **action object** (used in `precondition` and `norms[].action`) must contain:
- "dimension": one of ["control_flow", "data", "resource", "time"]
- "compliance_pattern": one of the patterns listed below (do NOT invent new patterns)
- Optional: include if relevant
  - "activities": list of strings
  - "resources": list of strings
  - "data_values": list of objects (with fields: "element", optional "value", "domain", "format", "purpose", "origin")
  - "temporal_constraints": list of objects (with keys like "interval_minutes", "min_duration_minutes", etc.)

---

IMPORTANT RULES:

1. **Label and Structure Consistency Across Versions**
   - Use the same labels (actions, activities, data, etc.) across versions unless the meaning changes.
   - If versions differ only in parameter values, keep the exact same structure — same compliance_pattern, nesting (e.g., precondition vs norms), and element names.
   - Do not switch between patterns like data_in_range ↔ data_in_domain unless the semantics change.

2. **Temporal Validity Defaults**
   - If the requirement does not specify a start/end: set
     - "start": "0000-01-01T00:00:00Z"
     - "end": "9999-12-31T23:59:59Z"

3. **Semantics of Norms**
   - What is not obligated is by default considered permitted but not prohibited.
   - Only actions explicitly indicated as prohibited are treated as "prohibition".
   - Therefore, absence of an "obligation" does not imply prohibition.
---

Now, based on the following requirement in plain text, return the corresponding JSON object (only the JSON):

"{text}"
